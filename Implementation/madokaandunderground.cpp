#include <bits/stdc++.h>
#define ll long long
#define forn(i, n) for (int i = 0; i < (int)(n); i++)

using namespace std;

int main()
{
#ifdef _DEBUG
    freopen("input.txt", "r", stdin);
    //	freopen("output.txt", "w", stdout);
#endif

    int t = 1;
    // cin>>t;
    while (t--)
    {
        int n, k, r, c;
        cin >> n >> k >> r >> c;
        
    }
    return 0;
}